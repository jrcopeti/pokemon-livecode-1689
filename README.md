# Pokemon livecode #1689

In your terminal, run the following commands:

- Clone the repository
- Go to the project's folder `cd pokemon-livecode-1689`
- Install the packages running `npm install`
- Start the server `npm run dev`
- Open your browser and usually is `http://localhost:5173`
